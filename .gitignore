# Raw downloads from claude.ai — the per-conversation files under chats/ are
# the tracked copy, not the source dump
*.zip
data-export*/
conversations.json
claude-conversation*.json

.DS_Store
__pycache__/
*.pyc
